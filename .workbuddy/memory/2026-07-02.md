# 2026-07-02 工作记录

## 技术栈分析
- 项目：投药量计算工具 (dose-calculator)
- React 18 + Vite 5 + ExcelJS + Vitest
- 纯 CSS Variables 设计系统（无 UI 框架）
- File System Access API 做本地文件持久化
- IndexedDB 存储文件句柄
- 单 HTML 文件输出（inline-build.mjs 内联所有资产）

## Skill 安装
通过 SkillHub 安装了 3 个技能：
1. **react** (slug: react) — React 19 工程化指南
2. **zustand-patterns** (slug: zustand-patterns) — Zustand 状态管理实战模式
3. **frontend-design** (slug: frontend-design-3) — 生产级前端界面设计

## Bug 修复：手机浏览器无法使用
**根因**：File System Access API (showOpenFilePicker) 在移动端浏览器不支持
**方案**：新增 localStorage 后备存储，自动降级
- 新建 `src/lib/localStorage.js` — localStorage 读写封装
- 修改 `src/store/useProjects.js` — 启动时检测 API 支持度，自动选择存储方式
  - 桌面端：优先 File System API
  - 移动端：自动降级到 localStorage
  - 新增 `ls-ready` / `ls-no-file` 状态
- 修改 `src/App.jsx` — 支持 `ls-ready` 状态渲染
- 修改 `src/components/FilePrompt.jsx` — 新增 `ls-no-file` 引导界面
- 修改 `index.html` — 添加移动端 meta 标签（viewport-fit=cover, apple-mobile-web-app）
- 添加 `gate-hint` CSS 样式

全部 53 个测试通过，构建成功。

## 改进方案实施（按优先级）
提交 b2a2821 推送到 GitHub，包含以下优化：

### P0 — 关键性能优化
1. **useStore.js 用 immer 重构** — 深层嵌套更新从6层spread改为可变更新，性能大幅提升
2. **代码分割** — 首屏JS从1.1MB降至18KB（减少98%）
   - ExcelJS 按需加载（点击导出/导入时才下载938KB）
   - 垛位图 WarehouseMap 使用 React.lazy + Suspense
   - exportExcel、importData 全部动态 import
3. **localStorage 版本校验** — 加入 _lsv 版本号和 migrateData 迁移机制

### P1 — 质量改进
4. **Canvas DPI 适配** — DrawPanel 和 canvasLib 添加 devicePixelRatio 支持
5. **PNG 导出使用2x渲染** — 提升导出图片质量

### P2 — 体验优化
6. **导出 Loading 状态** — 按钮显示"导出中…"，防止重复点击
7. **垛位图加载** — 添加 Suspense fallback（loading spinner）

### Bug修复
- 修复 SummaryBar 在 detail view 和 main view 之间 `exporting` prop 不一致
- 修复 vite.config.js inlineDynamicImports 阻止代码分割的问题

## 优化从数据生成垛位图形（提交 e48bab7）
修复多项问题：
1. 过滤体积为0的空垛位 — 只生成有效数据的垛位图形
2. 按区（Zone）分组布局 — 每个区有独立的标签和行
3. 基于物理占地面积（长×宽）缩放 slot 尺寸，不再用体积
4. slot 最小32×28px，最大90px，避免过小不可读
5. 小于32px的slot不渲染标签文字，防止挤在一起
6. DataPanel预览canvas支持DPR，修复模糊问题
7. DataPanel改为16:9自适应比例
8. 数据检测改为检查有效体积 > 0
